interface:
  display_name: "Claude Code Multi-Provider"
  short_description: "Audit and maintain Claude Code provider routing"
  brand_color: "#2563EB"
  default_prompt: "Use $claude-code-multi-provider to audit my Claude Code router setup without printing secrets."

policy:
  allow_implicit_invocation: true
