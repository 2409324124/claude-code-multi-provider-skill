[project]
name = "claude-code-multi-provider-router"
version = "1.0.0"
description = "Intelligent multi-provider router for Claude Code with fallback, cooldown, and stats tracking"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn>=0.34.0",
    "httpx>=0.25.0",
    "python-dotenv>=1.0.0",
]
